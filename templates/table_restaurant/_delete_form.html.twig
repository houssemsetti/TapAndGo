<form method="post" action="{{ path('app_table_restaurant_delete', {'idTable': table_restaurant.idTable}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ table_restaurant.idTable) }}">
    <button class="btn">Delete</button>
</form>
